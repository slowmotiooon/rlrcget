use crate::AppContext;
use color_eyre::eyre::{Ok, Result};
use ratatui::Frame;

pub fn draw(context: &mut AppContext, frame: &mut Frame) -> Result<()> {
    Ok(())
}

pub fn handle_events(context: &mut AppContext) -> Result<()> {
    Ok(())
}
